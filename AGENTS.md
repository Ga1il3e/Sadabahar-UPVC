## Learned User Preferences
- Prefers Apple-like, smooth scroll-scrubbed animations for hero storytelling.
- Prefers media transitions to be scroll-driven only, with no autoplay on initial page load.
- Prefers iterative UI refinement while preserving the current setup instead of large structural rewrites.

## Learned Workspace Facts
- Main website work centers on `sadabahar-upvc-landing.html` with `index.html` as entry.
- Animation assets and extracted frame sequences are kept under `animation_gif/`.
- Local project runtime tooling includes a portable Node install at `.tools/node/`.
